/**
 * Placeholder Database type.
 * Replace this with the generated types from Supabase CLI once the schema is defined:
 *   npx supabase gen types typescript --project-id <id> > types/database.ts
 */
export type Database = any
